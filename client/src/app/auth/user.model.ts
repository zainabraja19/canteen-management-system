export class User {
    constructor(
        private email: string,
        private empId: string,
        private name: string,
        private phone: string,
        private role: string,
        private _id: string
    ) { }
}